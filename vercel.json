{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npx --no-install vite build",
        "outputDirectory": "dist"
      }
    }
  ],
  "buildCommand": "npx --no-install vite build"
}
